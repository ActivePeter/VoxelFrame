#include "ChunkMap.h"
//chunkmap下是一个chunk的键值对
//用于存储chunks,并在操作时可以快速地获取
// #include "sparsepp/spp.h"

#include "Chunk.h"

using phmap::flat_hash_map;

ChunkMap::ChunkMap()
{
    // this->chunkPhMap[KeyStruct(0, 0, 0)] = new Chunk(0, 0, 0);
}

ChunkMap::~ChunkMap()
{
}
